import { card } from './card.interface';

// you can export any var also, but only you need to declare with let or const and then the type, and yes use equal to sign like we do for var
export let Cards: card[] = [
    
]

